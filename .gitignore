# Data Pump dumps — too large for git
data/*.dmp
data/*.log

# Run artifacts — regenerated by collector.run / assess.run / assess.report
collector/output/
assess/output/
sizing/output/

# Per-machine console preferences: which checks are on, and custom ones
web/settings.json
web/target_region.json

# Python
__pycache__/
*.py[cod]
.venv/
venv/
.env

# Node / CDK
node_modules/
cdk.out/

# Credentials — never commit
*.pem
credentials*
*secrets*
.aws/
*.credentials
aws_session*
*.tfvars

# Phase 4 planner output
remediate/output/

# Phase 4b conversion output (plan, generated PL/pgSQL, model audit rows)
convert/output/

# Phase 10 report output
report/output/

# Phase 5 gate output
blocker/output/

# Second-estate run artifacts (scripts/telco-source/run_phases.ps1)
telco-output/
dms/output/

# Phase 2, SCT path: SCT's own project, PDF and CSV, plus the run record.
# Never committed -- the project directory holds source connection details and
# the reports describe a real estate's schema.
sct/output/

# Phase 4d application-SQL output (plan plus generated PostgreSQL)
appsql/output/
